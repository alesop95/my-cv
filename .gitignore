# Materiali privati e non versionati
_notes/
attachments/
CLAUDE.local.md
.claude/settings.local.json

# Estratti temporanei
_notes/.tmp-docx-*/
.tmp-docx-*/

# LaTeX: derivati da compilazione (il .tex e' la fonte)
*.aux
*.log
*.out
*.toc
*.lof
*.lot
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.blg
*.bcf
*.run.xml
*.nav
*.snm
*.vrb
*.xmpi

# PDF: cv-sopranzi-alessio-{en,it,es}.pdf versionati deliberatamente (ADR-004, 2026-07-06,
# esteso alle tre lingue il 2026-07-08), per avere un link stabile e sempre allineato al CV
# compilato in ciascuna lingua direttamente su GitHub. Rigenerati insieme da scripts/build.ps1
# (o .sh): da lanciare prima di ogni commit che tocca main.tex, cosi' le tre lingue non si
# disallineano mai tra loro. Riga *.pdf NON attivata di proposito.

# Le istantanee datate per lingua (scripts/build-multilang.ps1/.sh) restano invece locali: si
# accumulano nel tempo (tre file nuovi per ogni giorno di build), a differenza di main.pdf che e'
# un singolo file che si aggiorna. Archivio personale, non pensato per finire in git (ADR-005 in
# .claude/memory/decisions.md). Raccolte in dated-builds/<lingua>/ dal 2026-07-07 invece che
# sciolte nella radice, per non mescolarle dopo settimane di build.
dated-builds/

# I derivati di compilazione vanno in build/ dal 2026-09-04, invece di stare sciolti nella radice:
# tredici file per tre lingue (.aux, .log, .out, .synctex.gz piu' pdfa.xmpi) che le regole per
# estensione qui sopra coprivano gia', ma la cartella contiene anche la copia di lavoro dei tre PDF
# prodotta da pdflatex prima che scripts/build.ps1 la riporti in radice. Quelle copie NON devono
# entrare in git: la riga *.pdf e' disattivata di proposito perche' i tre PDF in radice sono
# versionati (ADR-004, ADR-006), quindi senza questa riga le copie dentro build/ verrebbero
# committate come file nuovi.
build/

# Ambiente
.venv/
__pycache__/
*.pyc
.DS_Store
Thumbs.db
desktop.ini
